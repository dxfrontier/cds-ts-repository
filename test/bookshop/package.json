{
  "name": "@capire/orders",
  "version": "1.0.0",
  "scripts": {
    "start": "cds watch",
    "build:entities": "cds-typer '*' --outputDirectory ./srv/util/types/entities && npm run clean",
    "clean": "npx rimraf ./srv/util/types/entities/{*.js,**/*.js,**/**/*.js,**/**/**/*.js}"
  },
  "dependencies": {
    "@sap/cds": ">=7.3.0"
  },
  "devDependencies": {
    "@cap-js/cds-typer": "^0.11.1",
    "@cap-js/sqlite": "1.3.1"
  }
}
