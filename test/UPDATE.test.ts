import cds from '@sap/cds';
import path from 'path';

// const project = path.join(__dirname, '..', 'project');

// cds.test(project).verbose(true);

describe('UPDATE', () => {
  describe('.update()', () => {
    test('2 + 2 is 4', () => {
      expect(2 + 2).toBe(4);
    });
  });

  describe('.updateAllBy()', () => {
    test('2 + 2 is 4', () => {
      expect(2 + 2).toBe(4);
    });
  });

  describe('.updateLocaleTexts()', () => {
    test('2 + 2 is 4', () => {
      expect(2 + 2).toBe(4);
    });
  });
});
